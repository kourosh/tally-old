    <div class="row rowspace">
      <div class="col-md-3">
        <div class="pol-pic">
          <img src="/assets/200x250/<%= @event.pol.bioguide_id %>.jpg" class="img-responsive">
          <p class="party"><%= link_to "#{@event.pol.title}. #{@event.pol.fullname}", pol_path(@event.pol_id) %><br>(<%= @event.pol.party %>) <%= @event.pol.state %></p>
        </div>
      </div>
      <div class="col-md-9">
        <h2><a href="<%= @event.source %>"><%= @event.headline %></a></h2>
        <div class="button-div">
          <a href="#" class="btn btn-lg btn-success buttonz support" id="<%= @event.id %>">Support</a>
          <a href="#" class="btn btn-lg btn-danger buttonz oppose" id="<%= @event.id %>">Oppose</a>
        </div>
      </div>
    </div>

<ul id="comments">
  <% @comments.each do |comment| %>
    <li><%= comment.content %></li>
  <% end %>
</ul>

<h2>New Comment</h2>
<%= form_for [@event, Comment.new] do |form| %>
  <div class="formList">
    <p>
      <%= form.label :content %>
      <%= form.text_area :content, :rows => 5 %>
    </p>
    <p><%= submit_tag "Add comment" %></p>
  </div>
<% end %>